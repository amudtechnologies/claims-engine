name: Build Lifecycle

on:
  workflow_dispatch:
    inputs:
      bucket:
        description: "S3 bucket"
        required: true
        default: "amud-technologies"
      staging_prefix:
        description: "S3 prefix staging Parquet was written under"
        required: true
        default: "staging/jd_published_deposit"

jobs:
  build-lifecycle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Build core/{claim,observation,claim_party} (Phase 4)
        run: >
          uv run claims-engine build-lifecycle
          "${{ inputs.bucket }}"
          --staging-prefix "${{ inputs.staging_prefix }}"
